Off-canvas nav drawer with scrim; slides from left or right. Use .bp-mdrawer__link for items.

```jsx
<MobileDrawer open={open} onClose={fn} title="BIM·GIS Atlas"><a className="bp-mdrawer__link" aria-current="page">Projects</a></MobileDrawer>
```