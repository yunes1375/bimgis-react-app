Underline tabs with optional count badges; controlled or uncontrolled.

```jsx
<Tabs items={[{value:'model',label:'Model',count:248},{value:'layers',label:'GIS Layers',count:12}]} defaultValue="model" />
```