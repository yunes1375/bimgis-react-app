Surface container with optional header (title/subtitle/action) and footer. `accent` draws the teal→orange BIM·GIS top hairline.

```jsx
<Card variant="accent" title="Riverside Tower" subtitle="IFC · 248 elements" action={<Pill tone="ok">Aligned</Pill>}>…</Card>
```
Variants: default · raised · accent. Props: dense, footer.