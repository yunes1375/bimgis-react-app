Primary action control; teal "primary" for the main commit, "danger" for destructive, "ghost" for low-emphasis.

```jsx
<Button variant="primary" leftIcon="↥">Upload IFC</Button>
```
Variants: default · primary · secondary · ghost · danger. Sizes: sm · md · lg. Props: fullWidth, loading, leftIcon/rightIcon, disabled.