Labelled text field with mono uppercase label, optional hint, error and end-addon.

```jsx
<Input label="Project name" placeholder="Riverside Tower" fullWidth />
```
Props: label, hint, error, fullWidth, addonEnd + native input attrs.